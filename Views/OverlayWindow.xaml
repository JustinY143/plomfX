<Window x:Class="plomfX.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OverlayWindow"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Left="0" Top="0" Width="{Binding Source={x:Static SystemParameters.PrimaryScreenWidth}}" 
        Height="{Binding Source={x:Static SystemParameters.PrimaryScreenHeight}}">
    
    <Grid>
        <!-- Center the crosshair image absolutely -->
        <Image x:Name="CrosshairImage"
               Stretch="None"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               RenderOptions.BitmapScalingMode="HighQuality"
               RenderTransformOrigin="0.5,0.5">
            <Image.RenderTransform>
                <ScaleTransform x:Name="CrosshairScaleTransform" ScaleX="1" ScaleY="1"/>
            </Image.RenderTransform>
        </Image>
    </Grid>
</Window>