<UserControl x:Class="plomfX.Views.UserControls.CrosshairSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300">
    <Border Background="{DynamicResource PrimaryBackgroundBrush}" 
            BorderBrush="{DynamicResource PrimaryBorderBrush}" 
            BorderThickness="1" 
            CornerRadius="4"
            Padding="10">
        <StackPanel>
            <Grid Margin="0,0,0,10">
                <TextBlock Text="CROSSHAIR SETTINGS" 
                           Foreground="{DynamicResource SecondaryAccentBrush}" 
                           FontWeight="SemiBold" 
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"/>
                <Button x:Name="BackButton" 
                        Content="← Back" 
                        Style="{StaticResource ActionButtonStyle}" 
                        HorizontalAlignment="Right" 
                        Padding="8,4"
                        Click="BackButton_Click"/>
            </Grid>
            <!-- Scale Slider -->
            <StackPanel Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Scale:" Foreground="White" Width="60"/>
                    <TextBlock x:Name="ScaleValueText" Foreground="White" Text="100%"/>
                </StackPanel>
                <Slider x:Name="ScaleSlider" Minimum="0.25" Maximum="3.0" Value="1.0" TickFrequency="0.05" IsSnapToTickEnabled="True"/>
            </StackPanel>
            
            <!-- Opacity Slider -->
            <StackPanel Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Opacity:" Foreground="White" Width="60"/>
                    <TextBlock x:Name="OpacityValueText" Foreground="White" Text="100%"/>
                </StackPanel>
                <Slider x:Name="OpacitySlider" Minimum="0.1" Maximum="1.0" Value="1.0" TickFrequency="0.05" IsSnapToTickEnabled="True"/>
            </StackPanel>
            
            <!-- Color Tint -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Tint:" Foreground="White" Width="60" VerticalAlignment="Center"/>
                <Button x:Name="ResetColorButton" Content="↺" Width="28" Height="28" Margin="0,0,8,0" 
                    Style="{StaticResource ActionButtonStyle}" ToolTip="Reset to White"/>
                <Button x:Name="PickColorButton" Content="Choose Color..." Padding="8,4" Style="{StaticResource ActionButtonStyle}"/>
                <Rectangle x:Name="ColorPreview" Width="20" Height="20" Fill="White" Margin="10,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>