<Window x:Class="plomfX.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="200" Width="350"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="General Settings" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryForegroundBrush}" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Monitor:" Foreground="{DynamicResource SecondaryForegroundBrush}" Margin="0,0,0,5"/>
            <ComboBox x:Name="MonitorComboBox" DisplayMemberPath="DeviceName" 
                      Background="{DynamicResource SecondaryAccentBrush}"
                      Foreground="Black"
                      BorderThickness="0" Height="28"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,15,0,0">
            <Button Content="Save" Width="70" Style="{StaticResource ActionButtonStyle}" Click="Save_Click" Margin="0,0,10,0"/>
            <Button Content="Cancel" Width="70" Style="{StaticResource ActionButtonStyle}" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>